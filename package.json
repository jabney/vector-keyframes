{
  "name": "vector-keyframes",
  "version": "0.2.12",
  "description": "Interpolate between any number of 2d or 3d vector keyframes",
  "keywords": [
    "vector",
    "keyframe",
    "interpolation",
    "color"
  ],
  "main": "index.js",
  "repository": "git@github.com:jabney/vector-keyframes.git",
  "author": "James Abney <james.abney@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.4.0",
    "codecov": "^2.2.0",
    "istanbul": "^0.4.5",
    "lodash": "^4.17.4",
    "mocha": "^3.3.0"
  },
  "dependencies": {},
  "scripts": {
    "build": "babel src -d . --source-maps",
    "test": "npm run build && istanbul cover node_modules/mocha/bin/_mocha && codecov",
    "prepublish": "npm run build"
  }
}
